# ASR 主链 SSOT V1.1 测试报告（dialog_200 × 50）

版本：V1.1  
日期：2026-05-29  
测试范围：ASR 主链 SSOT 改造 + FW Detector v1 契约  
音频集：`D:\Programs\github\lingua_1\test wav\dialog_200`（manifest 前 50 条）  
原始结果：`electron_node/electron-node/tests/fw-detector-dialog-200-batch-result.json`

---

## 1. 测试环境

| 项 | 值 |
|----|-----|
| 节点 test server | `http://127.0.0.1:5020` |
| PROJECT_ROOT | `D:\Programs\github\lingua_1` |
| ASR 引擎 | `faster-whisper-vad`（50/50） |
| 模式 | `is_manual_cut: true`，`use_lexicon: true` |
| KenLM | `enableKenLMGate: true`，`weak_veto`，threshold `-0.2` |
| 批测命令 | `node tests/run-fw-detector-dialog-200-batch.js "<dialog_200>" --limit 50` |

---

## 2. 单元 / 门禁测试

| 套件 | 结果 |
|------|------|
| `npm run test:pipeline` | **6 suites / 9 tests PASS** |
| `npm run test:fw-detector` | **16 suites / 55 tests PASS** |
| `node scripts/fw-detector-gate.mjs` | **PASS** |

重点契约用例：

- `freeze-contract.test.ts` — 主链禁止 `repairedText` / `syncRepairedTextBaseline`
- `segment-not-overwritten.test.ts` — FW apply 后 segment 写锁
- `finalize-complete-aggregation.test.ts` — turn finalize 完整文本

---

## 3. dialog_200 批测汇总

**执行时间：** 2026-05-29T17:06:28Z  
**结论：** ✅ **50 / 50 PASS**（pipeline_ok_rate = 100%）

| 指标 | 值 |
|------|-----|
| total | 50 |
| pass | 50 |
| fail | 0 |
| skip | 0 |
| fw_triggered_count | 50 |
| fw_applied_case_count | 4 |
| fw_applied_total | 5 |
| fw_candidate_total | 18 |
| text_changed_count | 4 |
| lexicon_runtime_ok_count | 50 |
| kenlm_approved_total | 18 |
| kenlm_vetoed_total | 0 |
| false_repair_count | 4 |

### 3.1 分场景（by_scenario）

| scenario | pass | fail | fw_applied_cases |
|----------|------|------|------------------|
| cafe | 6 | 0 | 4 |
| meeting | 5 | 0 | 0 |
| taxi | 3 | 0 | 0 |
| hospital | 3 | 0 | 0 |
| shopping | 3 | 0 | 0 |
| friend | 3 | 0 | 0 |
| tech_deploy | 3 | 0 | 0 |
| customer_service | 3 | 0 | 0 |
| interview | 3 | 0 | 0 |
| classroom | 3 | 0 | 0 |
| hotel | 3 | 0 | 0 |
| bank | 3 | 0 | 0 |
| restaurant | 3 | 0 | 0 |
| gym | 3 | 0 | 0 |
| lexicon_homophone | 3 | 0 | 0 |

### 3.2 契约检查项（每条 case）

全部通过，无以下失败：

- `empty_text_asr` / `missing_raw_asr_text`
- `wrong_asr_service`
- `missing_fw_detector` / `missing_fw_summary`
- `ctc_nbest_present`
- `sentence_repair_should_not_run`
- `window_candidates_present`
- `lexicon_not_ok`

### 3.3 SSOT 观测样例（d001）

| 字段 | 值 |
|------|-----|
| `extra.raw_asr_text` | 你好,我想点一杯热拿铁**钟贝**少糖 深便温 |
| `text_asr` | 你好,我想点一杯热拿铁**中杯**少糖 深便温 |
| `fw_applied_count` | 1 |
| `text_changed` | true（segment 经 FW apply，raw 保持不变） |

符合 V1.1：`rawAsrText` immutable，`text_asr` 来自 `segmentForJobResult`。

---

## 4. 未覆盖项

| 项 | 说明 |
|----|------|
| dialog_200 全量 200 条 | 本次仅 `--limit 50` 标准验收 |
| multi-chunk turn finalize E2E | 需调度器 + 多 job 集成场景 |
| UI 手动回归 | 本次以 test server HTTP 批测为主 |

---

## 5. 启动说明（批测前置）

1. `cd electron_node/electron-node && npm run build`（含 `build:renderer`，否则 Electron 页面空白）
2. 设置 `PROJECT_ROOT` 后 `npm start` 或 `.\scripts\start_electron_node.ps1`
3. 等待 `:5020/health` 就绪后执行批测脚本

---

## 6. 总结

**ASR 主链 SSOT V1.1 改造通过单元测试、fw-gate 与 dialog_200（50 条）契约批测。**  
FW 在 cafe 等同音场景正常 apply；KenLM weak_veto 无 veto 记录；主链未触发 sentence_repair / window_candidates 违规路径。
