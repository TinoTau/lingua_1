# ASR→FW 主链冻结补丁 测试报告（dialog_200 × 50）

版本：V1.0  
日期：2026-05-30  
测试范围：冻结补丁（resolveBusinessAsrText、冻结合约、multi-chunk finalize）  
音频集：`D:\Programs\github\lingua_1\test wav\dialog_200`（manifest 前 50 条）  
原始结果：`electron_node/electron-node/tests/fw-detector-dialog-200-batch-result.json`

---

## 1. 测试环境

| 项 | 值 |
|----|-----|
| 构建 | `npm run build`（main + renderer） |
| 节点 test server | `http://127.0.0.1:5020` |
| PROJECT_ROOT | `D:\Programs\github\lingua_1` |
| ASR 引擎 | `faster-whisper-vad`（50/50） |
| 模式 | `is_manual_cut: true`，`use_lexicon: true` |

---

## 2. 单元 / 门禁测试

| 套件 | 结果 |
|------|------|
| `npm run test:pipeline` | **7 suites / 10 tests PASS** |
| `npm run test:fw-detector` | **16 suites / 59 tests PASS** |
| `npm run test:contract` | **11 tests PASS** |
| `node scripts/fw-detector-gate.mjs` | **PASS**（含 SSOT 静态检查） |

新增/重点用例：

| 用例 | 验证点 |
|------|--------|
| `post-asr-routing.test.ts` | segment → asrText fallback；禁止第三源 |
| `result-builder.test.ts` | `text_asr === getTextForTranslation(ctx)` |
| `multi-chunk-turn-finalize.test.ts` | 中间 chunk defer；finalize 合并「第一段 第二段」 |
| `freeze-contract.test.ts` | DEDUP 在 TRANSLATION 前；`resolveBusinessAsrText`；无 repairedText |

---

## 3. dialog_200 批测汇总

**执行时间：** 2026-05-30T08:20:39Z  
**结论：** ✅ **50 / 50 PASS**（pipeline_ok_rate = 100%）

| 指标 | 值 |
|------|-----|
| total / pass / fail / skip | 50 / 50 / 0 / 0 |
| fw_triggered_count | 50 |
| fw_applied_case_count | 4 |
| fw_applied_total | 5 |
| text_changed_count | 4 |
| lexicon_runtime_ok_count | 50 |
| kenlm_approved_total | 18 |
| kenlm_vetoed_total | 0 |
| false_repair_count | 4 |
| asr_service_id | faster-whisper-vad × 50 |

### 3.1 分场景

| scenario | pass | fw_applied_cases |
|----------|------|------------------|
| cafe | 6 | 4 |
| meeting | 5 | 0 |
| 其余 13 场景 | 各 3 | 0 |

### 3.2 契约检查（每条 case）

全部通过，无以下失败：

- `empty_text_asr` / `missing_raw_asr_text`
- `wrong_asr_service` / `missing_fw_detector`
- `ctc_nbest_present` / `sentence_repair_should_not_run`
- `window_candidates_present` / `lexicon_not_ok`

### 3.3 SSOT 观测（d001 样例）

| 字段 | 说明 |
|------|------|
| `extra.raw_asr_text` | ASR 原文（immutable） |
| `text_asr` | 来自 `segmentForJobResult`（FW apply 后） |
| `fw_applied_count` | 1（「钟贝→中杯」类修复） |

manual_cut 路径 segment 非空，批测契约路径 **不应出现** `source=asrText` fallback。

---

## 4. 启动前置

```powershell
cd D:\Programs\github\lingua_1\electron_node\electron-node
npm run build   # 含 renderer，避免 UI 空白
$env:PROJECT_ROOT = "D:\Programs\github\lingua_1"
npm start
# 或 .\scripts\start_electron_node.ps1
```

批测：

```powershell
node tests/run-fw-detector-dialog-200-batch.js "D:\Programs\github\lingua_1\test wav\dialog_200" --limit 50
```

---

## 5. 未覆盖项

| 项 | 说明 |
|----|------|
| dialog_200 全量 200 条 | 标准验收为 50 条 |
| 调度器 + Web 全链路 E2E | 本次以 test server HTTP 批测为主 |

---

## 6. 总结

**冻结补丁通过全部单元测试、fw-gate 与 dialog_200（50 条）契约批测。**  
NMT / `text_asr` 已统一为 `resolveBusinessAsrText` 同源链；multi-chunk finalize 有单测覆盖；主链冻结门禁全部满足。
