# ASR→FW 主链 P0/P1/P2 清理 测试报告（dialog_200 × 50）

版本：V1.0  
日期：2026-05-30  
测试范围：P0 fallback 删除、P1 FW 最小 extra、P2 Recover legacy + finalText wire  
音频集：`D:\Programs\github\lingua_1\test wav\dialog_200`（manifest 前 50 条）  
原始结果：`electron_node/electron-node/tests/fw-detector-dialog-200-batch-result.json`

---

## 1. 测试环境

| 项 | 值 |
|----|-----|
| 清理 | `cleanup_orphaned_processes_simple` + `clear_code_cache_comprehensive` + `clear_logs_simple` |
| 构建 | `npm run clear-cache` → `npm run build`（main + renderer） |
| 节点 test server | `http://127.0.0.1:5020` |
| PROJECT_ROOT | `D:\Programs\github\lingua_1` |
| ASR 引擎 | `faster-whisper-vad`（50/50） |
| 模式 | `is_manual_cut: true`，`use_lexicon: true` |
| KenLM | `weak_veto`，path 已加载 |

---

## 2. 单元 / 门禁测试

**执行时间：** 2026-05-30（批测前）

| 套件 | 结果 |
|------|------|
| `node scripts/fw-detector-gate.mjs` | **PASS**（无 fallback；FW 不 import legacy/recover） |
| `npm run test:pipeline` | **7 suites / 10 tests PASS** |
| `npm run test:fw-detector` | **16 suites / 61 tests PASS** |
| `npm run test:contract` | **10 tests PASS**（含 `legacy/recover/recover-contract.test.ts`） |

重点用例：

| 用例 | 验证点 |
|------|--------|
| `post-asr-routing.test.ts` | 仅 segment；无 asrText fallback |
| `aggregation-step.test.ts` | segment 空不 fallback asrText |
| `result-builder.test.ts` | FW extra 无 Recover 字段；segment 空不 fallback |
| `translation-step.test.ts` | segment 空 skip NMT |
| `freeze-contract.test.ts` | 无 repairedText；FW 不 import legacy |
| `multi-chunk-turn-finalize.test.ts` | turn finalize 全文 segment |

---

## 3. dialog_200 批测汇总

**执行时间：** 2026-05-30T09:19:12.491Z  
**结论：** ✅ **50 / 50 PASS**（pipeline_ok_rate = 1.0）

| 指标 | 值 |
|------|-----|
| total / pass / fail / skip | 50 / 50 / 0 / 0 |
| fw_triggered_count | 50 |
| fw_applied_case_count | 4 |
| fw_applied_total | 5 |
| text_changed_count | 4 |
| lexicon_runtime_ok_count | 50 |
| kenlm_approved_total | 18 |
| kenlm_vetoed_total | 0 |
| false_repair_count | 4 |
| asr_service_id | faster-whisper-vad × 50 |

### 3.1 分场景

| scenario | pass | fw_applied_cases |
|----------|------|------------------|
| cafe | 6 | 4 |
| meeting | 5 | 0 |
| 其余 13 场景 | 各 3 | 0 |

### 3.2 契约检查（每条 case）

全部通过，批测结果 JSON 中 **无** `sentence_repair` / `window_candidates` 字段，无以下失败：

- `empty_text_asr` / `missing_raw_asr_text`
- `wrong_asr_service` / `missing_fw_detector`
- `ctc_nbest_present` / `sentence_repair_should_not_run`
- `window_candidates_present` / `lexicon_not_ok`

### 3.3 SSOT 观测（d001 样例）

| 字段 | 值 |
|------|-----|
| `raw_asr_text` | 你好,我想点一杯热拿铁**钟贝**少糖 深便温 |
| `text_asr` | 你好,我想点一杯热拿铁**中杯**少糖 深便温 |
| `fw_applied_count` | 1 |
| `lexicon_runtime_status` | ok |
| `pipeline_ms` | 5696 |

`text_asr` 来自 FW apply 后的 `segmentForJobResult`，与 `raw_asr_text` 分离，符合 P0/P1 契约。

### 3.4 FW apply 命中 case

| id | scenario | fw_applied | 说明 |
|----|----------|------------|------|
| d001 | cafe | 1 | 钟贝→中杯 |
| d002 | cafe | 2 | — |
| d046 | cafe | 1 | — |
| d047 | cafe | 1 | — |

---

## 4. 结论

| 维度 | 判定 |
|------|------|
| P0 fallback 删除 | ✅ 批测 + 单测通过 |
| P1 FW 最小 extra | ✅ 无 Recover 结构泄露 |
| P2 Recover 隔离 | ✅ 主链批测不受影响 |
| dialog_200 × 50 | ✅ 50/50 PASS |

**主链 P0/P1/P2 清理可宣布验收通过。** 后续优化限于 P1.3 词库 Coverage，不改控制流。

---

## 5. 复现命令

```powershell
cd D:\Programs\github\lingua_1
.\scripts\cleanup_orphaned_processes_simple.ps1
cd electron_node\electron-node
npm run build
cd D:\Programs\github\lingua_1
.\scripts\start_electron_node.ps1

cd electron_node\electron-node
$env:PROJECT_ROOT = "D:\Programs\github\lingua_1"
node tests/run-fw-detector-dialog-200-batch.js "D:\Programs\github\lingua_1\test wav\dialog_200" --limit 50
```
