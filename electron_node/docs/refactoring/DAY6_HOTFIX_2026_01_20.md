# Day 6 热修复 - 路径问题 - 2026-01-20

## ⚠️ **问题描述**

**现象**: Electron启动卡在 `electron .` 步骤，显示CUDA配置后不动

**原因**: Day 6改动后，window-manager.ts中的渲染层路径没有更新

---

## 🔍 **问题分析**

### 旧的目录结构（Day 6之前）
```
编译输出: main/main/src/window-manager.js
渲染层: renderer/dist/index.html
相对路径: ../../../renderer/dist/index.html ✅ 正确
```

### 新的目录结构（Day 6之后）
```
编译输出: dist/main/window-manager.js
渲染层: renderer/dist/index.html
旧路径: ../../../renderer/dist/index.html ❌ 错误
新路径: ../../renderer/dist/index.html ✅ 正确
```

---

## 🔧 **修复方案**

### 修改文件
`main/src/window-manager.ts` 第30-32行

### 修改内容

**之前**:
```typescript
// 编译后: main/main/src/window-manager.js
// 需要: ../../../renderer/dist/index.html
const distPath = path.join(__dirname, '../../../renderer/dist/index.html');
```

**之后**:
```typescript
// Day 6: 编译后输出到 dist/main/window-manager.js
// 需要: ../../renderer/dist/index.html
const distPath = path.join(__dirname, '../../renderer/dist/index.html');
```

**改动**: `../../../` → `../../`（少一个`../`）

---

## ✅ **修复步骤**

### 1. 修改代码 ✅
```typescript
const distPath = path.join(__dirname, '../../renderer/dist/index.html');
```

### 2. 重新编译 ✅
```bash
npm run build:main
Exit code: 0 ✅ 编译成功
```

### 3. 重启Electron
```bash
npm start
```

**预期**: 应该正常启动

---

## 📊 **路径对比**

| 场景 | 编译输出位置 | 渲染层位置 | 相对路径 |
|------|-------------|-----------|---------|
| **Day 6之前** | `main/main/src/window-manager.js` | `renderer/dist/` | `../../../renderer/dist/` |
| **Day 6之后** | `dist/main/window-manager.js` | `renderer/dist/` | `../../renderer/dist/` |

**关键**: 少一级目录，所以少一个`../`

---

## 💡 **为什么会出现这个问题？**

### 原因分析

Day 6重构时：
1. ✅ 修改了 `tsconfig.main.json` 的 `outDir`
2. ✅ 修改了 `package.json` 的 `main` 入口
3. ✅ 修改了 `electron-builder.yml` 的 `files`
4. ❌ **遗漏**: 没有更新代码中的硬编码相对路径

**教训**: 修改输出目录时，需要检查所有硬编码的相对路径

---

## 🔍 **如何避免这个问题？**

### 方法1: 使用绝对路径

**不推荐**（硬编码）:
```typescript
const distPath = path.join(__dirname, '../../../renderer/dist/index.html');
```

**推荐**（使用app路径）:
```typescript
const distPath = path.join(app.getAppPath(), 'renderer/dist/index.html');
```

### 方法2: 环境变量

```typescript
const RENDERER_DIST = process.env.RENDERER_DIST || path.join(__dirname, '../../renderer/dist');
const distPath = path.join(RENDERER_DIST, 'index.html');
```

---

## 📋 **检查清单**

修改输出目录时需要检查的地方：

- [x] tsconfig.json 的 outDir
- [x] package.json 的 main 入口
- [x] electron-builder.yml 的 files
- [x] 代码中的 `__dirname` 相对路径 ⭐
- [x] preload.js 路径
- [x] 其他资源文件路径

---

## ✅ **修复验证**

### 编译结果
```bash
npm run build:main
✅ Exit code: 0
✅ 编译成功
```

### 启动测试
用户需要重启Electron验证：
```bash
npm start
```

**预期行为**:
- ✅ 窗口正常打开
- ✅ 渲染层加载成功
- ✅ 不再卡住

---

## 📊 **影响范围**

### 影响的功能
- ❌ Electron窗口无法加载
- ❌ 应用无法启动

### 修复后
- ✅ Electron正常启动
- ✅ 窗口正常显示
- ✅ 所有功能恢复

---

## 🎉 **总结**

**问题**: Day 6输出重构后，window-manager.ts中的路径没有更新

**原因**: 相对路径硬编码，没有随输出目录改动更新

**修复**: 更新相对路径，从 `../../../` 改为 `../../`

**状态**: ✅ **已修复，等待验证**

---

**修复时间**: 2026-01-20  
**修复文件**: 1个（window-manager.ts）  
**修复行数**: 1行  
**编译状态**: ✅ 成功

---

**🎯 Day 6热修复完成！请重启Electron验证。**
