/**
 * Lexicon V3.1 Patch Apply E2E — executed via scripts/lexicon/run-patch-e2e-runner.mjs (Electron ABI).
 * Jest VM cannot load ESM term-materialize; keep this file as documentation anchor only.
 */
import { describe } from '@jest/globals';

describe.skip('lexicon-patch-v3 e2e (use npm run test:lexicon-patch-e2e)', () => {
  it('placeholder', () => {});
});
