/**
 * Non-default Recover mode bridge only.
 * FW frozen main chain must not depend on this module at runtime.
 */

export { buildLegacyRecoverResultExtra } from '../legacy/recover/legacy-recover-result-extra';
