# 备份代码启动脚本已创建（简化版）

**日期**: 2026-01-21 02:45  
**状态**: ✅ 完成

---

## 📁 创建的文件

### 1. 构建并启动脚本（推荐）

**文件**: `D:\Programs\github\lingua_1\expired\lingua_1-main\构建并启动备份代码.ps1`

**功能**:
- 清理缓存（调度器、Web端、节点端）
- 构建项目（Web端、节点端）
- 启动三端服务（调度器、节点端、Web端）

**使用**:
```powershell
cd D:\Programs\github\lingua_1\expired\lingua_1-main
.\构建并启动备份代码.ps1
```

---

### 2. 简单启动脚本

**文件**: `D:\Programs\github\lingua_1\expired\lingua_1-main\启动备份代码系统.ps1`

**功能**:
- 直接启动三端服务（不执行清理和构建）

**使用**:
```powershell
cd D:\Programs\github\lingua_1\expired\lingua_1-main
.\启动备份代码系统.ps1
```

---

### 3. 使用说明

**文件**: `D:\Programs\github\lingua_1\expired\lingua_1-main\启动说明.md`

---

## 🚀 快速开始

### 首次启动或需要重新构建

```powershell
cd D:\Programs\github\lingua_1\expired\lingua_1-main
.\构建并启动备份代码.ps1
```

**执行的命令**（按您提供的顺序）:

1. **调度服务器**:
   ```powershell
   cd D:\Programs\github\lingua_1\expired\lingua_1-main\central_server\scheduler
   cargo clean
   cd D:\Programs\github\lingua_1\expired\lingua_1-main
   .\scripts\start_scheduler.ps1
   ```

2. **节点端**:
   ```powershell
   cd D:\Programs\github\lingua_1\expired\lingua_1-main
   npm run clear-cache
   npm run build
   cd D:\Programs\github\lingua_1\expired\lingua_1-main
   .\scripts\start_electron_node.ps1
   ```

3. **Web端**:
   ```powershell
   cd D:\Programs\github\lingua_1\expired\lingua_1-main\webapp\web-client
   .\clean_cache.ps1
   npm install
   npm run build
   cd D:\Programs\github\lingua_1\expired\lingua_1-main
   .\scripts\start_webapp.ps1
   ```

---

### 快速重启（已构建过）

```powershell
cd D:\Programs\github\lingua_1\expired\lingua_1-main
.\启动备份代码系统.ps1
```

---

## 📊 集成测试流程

### 1. 启动备份代码

```powershell
cd D:\Programs\github\lingua_1\expired\lingua_1-main
.\构建并启动备份代码.ps1
```

等待所有服务启动完成（会打开3个终端窗口）

---

### 2. 进行测试

打开浏览器: **http://localhost:3000**

进行语音识别测试（使用相同的测试音频）

---

### 3. 查看性能日志

```powershell
# 查看ASR性能数据
Get-Content "D:\Programs\github\lingua_1\expired\lingua_1-main\electron_node\services\faster_whisper_vad\logs\faster-whisper-vad-service.log" | Select-String "phase=segments_list_done"
```

**关键指标**:
- `t_segments_list`: list(segments)耗时
- `audio_duration`: 音频时长
- `segments_count`: segments数量

---

### 4. 对比结果

**备份代码**:
```powershell
Get-Content "D:\Programs\github\lingua_1\expired\lingua_1-main\electron_node\services\faster_whisper_vad\logs\faster-whisper-vad-service.log" | Select-String "t_segments_list" | Select-Object -Last 1
```

**当前代码**:
```powershell
Get-Content "D:\Programs\github\lingua_1\electron_node\services\faster_whisper_vad\logs\faster-whisper-vad-service.log" | Select-String "t_segments_list" | Select-Object -Last 1
```

---

## 🎯 关键判断

### 如果性能相似（差异<20%）

```
✅ 结论: 问题是历史遗留，不是最近改动导致
→ 需要深度优化或架构改造
→ 参考: ASR_worker改造方案.md
```

### 如果备份代码快很多（差异>50%）

```
⚠️ 结论: 当前代码有性能退化
→ 需要对比代码差异
→ 找出导致退化的原因
```

---

## ⚠️ 注意事项

### 1. 端口冲突

备份代码使用默认端口（5010调度器，3000 Web端）。

**如果当前代码也在运行**，会有端口冲突。

**解决方法**: 
- 先停止当前代码的服务
- 或关闭当前代码的终端窗口

### 2. 停止服务

**方法1**: 关闭终端窗口（推荐）

**方法2**: 在终端按Ctrl+C

**方法3**: 强制停止所有
```powershell
Get-Process python, cargo, electron, node | Stop-Process -Force
```

---

## 📝 总结

您现在有两个脚本：

1. **构建并启动**（首次或需要重新构建时使用）
   - 完整的清理、构建、启动流程
   - 适合首次启动或代码有变更

2. **简单启动**（快速重启时使用）
   - 只启动服务，不清理和构建
   - 适合服务临时停止后重启

**推荐使用**: `构建并启动备份代码.ps1`（确保干净的环境）

---

**创建日期**: 2026-01-21 02:45  
**状态**: ✅ 可以使用
