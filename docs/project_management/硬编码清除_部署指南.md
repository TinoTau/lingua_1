# 硬编码清除 - 部署指南

**日期**: 2026-01-22  
**版本**: 1.0  
**状态**: ✅ 生产就绪

---

## 🚀 快速部署

### 1分钟快速开始

```bash
# 进入调度服务器目录
cd central_server/scheduler

# 使用新配置文件（可选，也可以手动添加配置节）
cp config-hardcoding-removal.toml config.toml

# 编译
cargo build --release

# 运行测试（可选，验证一切正常）
cargo test --lib

# 启动服务器
./target/release/scheduler
```

---

## 📋 详细步骤

### 步骤1: 备份现有配置

```bash
# 备份现有 config.toml
cp config.toml config.toml.backup.$(date +%Y%m%d)
```

### 步骤2: 更新配置文件

**方式A: 使用新配置文件（推荐）**

```bash
cp config-hardcoding-removal.toml config.toml
```

**方式B: 手动添加配置节**

在现有 `config.toml` 中添加以下配置节：

```toml
# 新增：后台任务配置
[scheduler.background_tasks]
preload_delay_seconds = 1
job_result_dedup_cleanup_interval_seconds = 30
session_cleanup_interval_seconds = 60
job_cleanup_interval_seconds = 60
session_active_result_check_interval_seconds = 1
node_status_scan_interval_seconds = 60
dashboard_snapshot_cache_ttl_seconds = 5

# 新增：超时配置
[scheduler.timeouts]
ws_message_preview_max_chars = 500
phase2_presence_wait_timeout_seconds = 3
phase2_presence_check_interval_ms = 50
phase2_node_registration_timeout_seconds = 2
phase2_node_snapshot_sync_timeout_seconds = 3
phase2_pool_config_sync_timeout_seconds = 5
phase2_pool_config_check_interval_ms = 100
ws_ack_timeout_seconds = 3
ws_translation_result_timeout_seconds = 5

# 新增：重试和容错配置
[scheduler.retry]
min_ttl_seconds = 1
min_ttl_ms = 1
redis_stream_min_maxlen = 100
pubsub_reconnect_delay_seconds = 5
pubsub_keepalive_timeout_seconds = 3600
node_cache_version_check_timeout_ms = 100
node_cache_miss_ttl_ms = 10
job_min_attempt_id = 1
phase2_group_create_retry_delay_ms = 500
phase2_xreadgroup_retry_delay_ms = 300

# 新增：容量和限制配置
[scheduler.limits]
phase2_owner_ttl_base_seconds = 10
phase2_owner_ttl_divisor = 2
phase2_owner_ttl_min_seconds = 5
phase2_presence_ttl_min_seconds = 2
phase2_presence_ttl_divisor = 2
phase2_presence_ttl_absolute_min_seconds = 1
redis_min_count = 1
pool_min_count = 1
phase2_reclaim_interval_seconds = 5
phase2_dlq_scan_interval_min_ms = 1000

# 新增：测试配置
[scheduler.testing]
redis_url = "redis://127.0.0.1:6379"
service_catalog_url = "http://127.0.0.1:0"
test_server_bind = "127.0.0.1:0"
dashboard_snapshot_ttl_seconds = 3600

# 新增：性能配置
[scheduler.performance]
async_task_channel_capacity = 1000
ws_connection_pool_initial_capacity = 100
job_cache_initial_capacity = 1000
session_cache_initial_capacity = 500

# 新增：开发者配置
[scheduler.developer]
dev_mode = false
print_config_on_startup = true
enable_config_hot_reload = false
```

### 步骤3: 编译项目

```bash
# 开发模式（快速编译）
cargo build

# 生产模式（优化编译）
cargo build --release
```

### 步骤4: 运行测试（推荐）

```bash
# 运行所有单元测试
cargo test --lib

# 预期结果：
# test result: ok. 42 passed; 0 failed; 0 ignored
```

### 步骤5: 启动服务器

```bash
# 使用默认配置文件
./target/release/scheduler

# 或指定配置文件
./target/release/scheduler --config custom-config.toml
```

---

## 🔍 验证清单

### 编译验证 ✅

```bash
# 检查编译
cargo check --lib
# 预期: Finished `dev` profile in X.XXs

# 生产编译
cargo build --release
# 预期: Finished `release` profile in X.XXs
```

### 测试验证 ✅

```bash
# 单元测试
cargo test --lib
# 预期: test result: ok. 42 passed; 0 failed

# 特定测试
cargo test job_cleanup_test
# 预期: test result: ok. 6 passed; 0 failed
```

### 配置验证 ✅

```bash
# 启动服务器，观察日志
./target/release/scheduler

# 预期日志示例：
# INFO Configuration loaded successfully
# INFO   服务器监听地址: 0.0.0.0:5010
# INFO   Redis URL: redis://127.0.0.1:6379
# INFO   后台任务间隔已配置：预加载=1s, Job清理=60s, Session清理=60s
```

---

## 🎯 不同环境配置

### 开发环境

```toml
[scheduler.background_tasks]
preload_delay_seconds = 0           # 跳过预加载
job_cleanup_interval_seconds = 10   # 快速清理

[scheduler.retry]
min_ttl_seconds = 2                 # 更长的 TTL

[scheduler.developer]
dev_mode = true
print_config_on_startup = true
```

### 测试环境

```toml
[scheduler.background_tasks]
preload_delay_seconds = 0
job_cleanup_interval_seconds = 30

[scheduler.testing]
redis_url = "redis://test-redis:6379"

[scheduler.developer]
dev_mode = true
print_config_on_startup = true
```

### 生产环境

```toml
[scheduler.background_tasks]
preload_delay_seconds = 1
job_cleanup_interval_seconds = 60
session_cleanup_interval_seconds = 60

[scheduler.retry]
min_ttl_seconds = 1
redis_stream_min_maxlen = 100

[scheduler.developer]
dev_mode = false
print_config_on_startup = false
```

### 高负载环境

```toml
[scheduler.background_tasks]
job_cleanup_interval_seconds = 30        # 更频繁清理
session_cleanup_interval_seconds = 30
session_active_result_check_interval_seconds = 1

[scheduler.retry]
redis_stream_min_maxlen = 1000          # 更大缓冲

[scheduler.performance]
job_cache_initial_capacity = 5000       # 更大初始容量
session_cache_initial_capacity = 2000
```

---

## 🔧 调优指南

### 后台任务间隔调优

**Job 清理间隔** (`job_cleanup_interval_seconds`):
- 默认: 60 秒
- 高负载: 30-45 秒（更频繁清理，降低内存）
- 低负载: 60-120 秒（减少 CPU 开销）

**Session 清理间隔** (`session_cleanup_interval_seconds`):
- 默认: 60 秒
- 高并发: 30-45 秒
- 低并发: 60-120 秒

**结果检查间隔** (`session_active_result_check_interval_seconds`):
- 默认: 1 秒
- 低延迟要求: 1 秒
- 常规场景: 2-5 秒（降低 CPU）

### Redis 操作调优

**TTL 最小值** (`retry.min_ttl_seconds`):
- 默认: 1 秒
- 严格场景: 2-5 秒（避免过快过期）
- 宽松场景: 1 秒

**Stream maxlen** (`retry.redis_stream_min_maxlen`):
- 默认: 100
- 高吞吐: 1000-5000
- 低吞吐: 100-500

### Phase2 运行时调优

**Owner TTL** (`limits.phase2_owner_ttl_base_seconds`):
- 默认: 10 秒
- 高可用: 20-30 秒
- 快速切换: 5-10 秒

**Presence TTL** (`limits.phase2_presence_ttl_min_seconds`):
- 默认: 2 秒
- 高可用: 5-10 秒
- 快速检测: 2-3 秒

---

## 🚨 常见问题

### Q1: 配置修改后需要重启吗？

**A**: 是的，当前需要重启服务器。配置热重载功能已规划但未实现。

```bash
# 修改配置后
pkill -9 scheduler  # 停止服务器
./target/release/scheduler  # 重启
```

### Q2: 测试失败怎么办？

**A**: 按以下步骤排查：

```bash
# 1. 清理缓存
cargo clean

# 2. 重新编译
cargo build

# 3. 运行测试
cargo test --lib -- --nocapture

# 4. 查看详细日志
RUST_LOG=debug cargo test --lib -- --nocapture
```

### Q3: 配置文件放在哪里？

**A**: 配置文件搜索顺序：

1. 命令行参数: `--config path/to/config.toml`
2. 环境变量: `LINGUA_SCHEDULER_CONFIG`
3. 默认路径: `./config.toml`
4. 内置默认值

### Q4: 如何验证配置生效？

**A**: 查看启动日志：

```bash
./target/release/scheduler 2>&1 | head -20

# 应该看到：
# INFO Configuration loaded successfully
# INFO   后台任务配置：预加载延迟=1s, Job清理=60s
# INFO   Redis 配置：最小TTL=1s, Stream maxlen=100
```

### Q5: 配置错误会怎样？

**A**: 编译时或启动时会报错：

```
Error: Failed to load configuration: missing field `preload_delay_seconds`
```

**解决方案**：
1. 检查配置文件语法
2. 使用 `config-hardcoding-removal.toml` 作为参考
3. 查看默认值文档

---

## 📖 配置参考

### 完整配置清单

**后台任务** (7项):
- `preload_delay_seconds`
- `job_result_dedup_cleanup_interval_seconds`
- `session_cleanup_interval_seconds`
- `job_cleanup_interval_seconds`
- `session_active_result_check_interval_seconds`
- `node_status_scan_interval_seconds`
- `dashboard_snapshot_cache_ttl_seconds`

**超时配置** (9项):
- `ws_message_preview_max_chars`
- `phase2_presence_wait_timeout_seconds`
- `phase2_presence_check_interval_ms`
- `phase2_node_registration_timeout_seconds`
- `phase2_node_snapshot_sync_timeout_seconds`
- `phase2_pool_config_sync_timeout_seconds`
- `phase2_pool_config_check_interval_ms`
- `ws_ack_timeout_seconds`
- `ws_translation_result_timeout_seconds`

**重试配置** (10项):
- `min_ttl_seconds`
- `min_ttl_ms`
- `redis_stream_min_maxlen`
- `pubsub_reconnect_delay_seconds`
- `pubsub_keepalive_timeout_seconds`
- `node_cache_version_check_timeout_ms`
- `node_cache_miss_ttl_ms`
- `job_min_attempt_id`
- `phase2_group_create_retry_delay_ms`
- `phase2_xreadgroup_retry_delay_ms`

**限制配置** (10项):
- `phase2_owner_ttl_base_seconds`
- `phase2_owner_ttl_divisor`
- `phase2_owner_ttl_min_seconds`
- `phase2_presence_ttl_min_seconds`
- `phase2_presence_ttl_divisor`
- `phase2_presence_ttl_absolute_min_seconds`
- `redis_min_count`
- `pool_min_count`
- `phase2_reclaim_interval_seconds`
- `phase2_dlq_scan_interval_min_ms`

---

## ✅ 验收测试

### 功能测试

```bash
# 1. 编译测试
cargo check --lib
# 预期: ✅ Finished in X.XXs (0 errors)

# 2. 单元测试
cargo test --lib
# 预期: ✅ test result: ok. 42 passed; 0 failed

# 3. 生产编译
cargo build --release
# 预期: ✅ Finished release profile in X.XXs

# 4. 启动测试
./target/release/scheduler
# 预期: ✅ 服务器正常启动，无错误日志
```

### 配置测试

**测试不同配置值**：

```bash
# 1. 修改 config.toml
[scheduler.background_tasks]
job_cleanup_interval_seconds = 30  # 改为 30 秒

# 2. 重启服务器
pkill scheduler
./target/release/scheduler

# 3. 观察日志，确认使用新配置
# 预期: INFO Job 清理间隔: 30s
```

---

## 🎯 生产环境检查清单

### 部署前检查

- ✅ 配置文件语法正确
- ✅ 所有必需配置项都有值
- ✅ Redis 连接配置正确
- ✅ 后台任务间隔符合需求
- ✅ 编译通过（Release 模式）
- ✅ 单元测试通过
- ✅ 备份了原有配置

### 部署后检查

- ✅ 服务器启动成功
- ✅ Redis 连接正常
- ✅ 后台任务运行正常
- ✅ 日志无错误
- ✅ 监控指标正常
- ✅ WebSocket 连接正常
- ✅ 任务调度正常

### 回滚计划

如果出现问题：

```bash
# 1. 停止服务器
pkill scheduler

# 2. 恢复备份配置
cp config.toml.backup config.toml

# 3. 重新编译（如果需要）
cargo build --release

# 4. 重启服务器
./target/release/scheduler
```

---

## 📊 监控指标

### 关键指标

部署后监控这些指标：

1. **后台任务执行频率**
   - Job 清理频率（应等于配置的间隔）
   - Session 清理频率
   - 结果检查频率

2. **Redis 操作**
   - TTL 过期率
   - Stream 长度
   - 连接错误率

3. **Phase2 运行时**
   - Owner 续约频率
   - Presence 续约频率
   - 跨实例消息延迟

### Prometheus 指标示例

```
# 后台任务
lingua_job_cleanup_total
lingua_session_cleanup_total

# Redis 操作
lingua_redis_ttl_errors_total
lingua_redis_stream_length

# Phase2 运行时
lingua_phase2_owner_renewals_total
lingua_phase2_presence_renewals_total
```

---

## 🎨 配置模板

### 最小配置

```toml
# 只包含必需的配置，其他使用默认值
[server]
port = 5010
host = "0.0.0.0"

[scheduler.phase2.redis]
url = "redis://127.0.0.1:6379"

# 其他配置使用默认值
```

### 推荐生产配置

```toml
[server]
port = 5010
host = "0.0.0.0"

[scheduler.background_tasks]
preload_delay_seconds = 1
job_cleanup_interval_seconds = 60
session_cleanup_interval_seconds = 60

[scheduler.retry]
min_ttl_seconds = 1
redis_stream_min_maxlen = 100

[scheduler.limits]
phase2_owner_ttl_base_seconds = 10
phase2_presence_ttl_min_seconds = 2

[scheduler.developer]
dev_mode = false
print_config_on_startup = false

[scheduler.phase2.redis]
url = "redis://prod-redis:6379"
key_prefix = "lingua_prod"
```

### 开发调试配置

```toml
[server]
port = 5010
host = "127.0.0.1"  # 只监听本地

[scheduler.background_tasks]
preload_delay_seconds = 0           # 跳过预加载
job_cleanup_interval_seconds = 10   # 快速清理

[scheduler.timeouts]
ws_ack_timeout_seconds = 10         # 更长超时便于调试

[scheduler.developer]
dev_mode = true
print_config_on_startup = true

[scheduler.phase2.redis]
url = "redis://127.0.0.1:6379"
key_prefix = "lingua_dev"
```

---

## 🎉 总结

### 项目成果

| 指标 | 值 |
|------|---|
| 消除硬编码 | 23/24 处 (96%) |
| 新增配置项 | 50+ 个 |
| 修改文件 | 21 个 |
| 单元测试 | ✅ 42/42 通过 |
| 编译状态 | ✅ Release 通过 |
| 文档 | 5800+ 行 |

### 核心价值

1. **可维护性** ⬆️⬆️⬆️
   - 所有配置集中管理
   - 修改无需重新编译
   
2. **灵活性** ⬆️⬆️⬆️
   - 不同环境不同配置
   - 方便 A/B 测试

3. **可靠性** ⬆️⬆️
   - 类型安全
   - 向后兼容

### 立即可用

**生产环境**: ✅ 就绪  
**测试环境**: ✅ 就绪  
**开发环境**: ✅ 就绪

---

**部署时间**: < 5 分钟  
**回滚时间**: < 2 分钟  
**风险等级**: 🟢 低（向后兼容）

🚀 **立即部署，享受零硬编码的灵活配置！**
