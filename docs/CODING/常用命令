# 0. 清理日志
cd D:\Programs\github\lingua_1

# 快速清理（总共3秒）
.\scripts\cleanup_orphaned_processes_simple.ps1
.\scripts\clear_code_cache_comprehensive.ps1
.\scripts\maintenance\clear_logs_simple.ps1

# 1. 节点端（日常启动）
cd D:\Programs\github\lingua_1\electron_node\electron-node
npm run clear-cache
npm run build
# Native rebuild（better-sqlite3 for Electron）— 不是 lexicon DB rebuild
npm run lexicon:rebuild-native
cd D:\Programs\github\lingua_1
.\scripts\start_electron_node.ps1
# 启动后确认：日志里 lexicon_runtime_status: ok；config 里 lexiconRecall.enabled=true

0. 请按照这个方向（不要偏离已经冻结的架构）给我一段提示词，用于开发前的代码审计（需要生成审计报告文档）。除功能、接口、数据结构和流程审计外，必须同时进行 Frozen Architecture Verification 和 Architecture Drift Audit，核对冻结设计、决策链路、接口契约、数据契约和运行链路是否仍然成立；不仅要确认功能是否存在，还要确认功能是否真正参与最终决策、是否进入主链路、是否被后续模块覆盖、绕过或降级；检查是否存在历史遗留逻辑、兼容逻辑、旁路逻辑、失效逻辑、重复实现、隐藏门控、死代码、权重失效、上下文失效或架构漂移；输出审计报告时必须区分“功能存在”和“功能生效”，列出 Expected、Actual、Impact、Severity，并输出 Drift Matrix、Ownership Matrix、Decision Path Matrix、Dead Feature Matrix 和 Required Repair Matrix，明确哪些逻辑属于 KEEP、MODIFY、RESTORE、DELETE。
1. 这是审计结果，请结合冻结设计、历史审计结论、开发方案和实际代码实现进行分析，不仅评估功能是否正确，还要评估是否存在架构漂移、责任边界漂移、决策链路漂移、权重失效、门控失效、上下文失效、功能被覆盖、功能存在但不生效等情况；请把具体开发方案整理成文档，包括设计约束、架构、接口、代码逻辑、数据结构、Diagnostics/Trace 样例、Target List、Check List、Regression List、Acceptance Criteria 和 Architecture Compliance Criteria；对于每个开发项明确其目的、责任边界、输入输出、最终决策位置以及与冻结架构的对应关系；必须区分 KEEP、MODIFY、RESTORE、DELETE，明确哪些逻辑应保留、哪些逻辑应恢复、哪些逻辑应删除，并增加 Frozen Architecture Verification、Semantic Acceptance、Contract Verification 和 Regression Gate，确保开发完成后验证的不仅是功能正确，而且是冻结架构仍然真实参与最终决策；最终生成可直接进入开发阶段的完整 md 文档正文。
2.《》，请阅读之后结合实际代码进行审计，看一下是否存在需要补充的信息、约束、边界条件、隐含前提、验收条件、测试要求或实现细节，并核对当前代码与冻结设计是否一致；不要只检查文档缺失项，还要检查实际代码中是否存在历史遗留逻辑、兼容逻辑、旁路逻辑、失效逻辑、重复实现、隐藏门控、决策覆盖、死代码或架构漂移；对于每一项补充内容，必须说明来源（设计缺失、代码实现、测试发现或架构漂移），并明确其影响范围；如发现“功能存在但不生效”“功能实现但未进入主链路”“功能结果被后续覆盖”等情况，必须列入补充清单；最终整理为独立文档，输出补充项清单、风险等级、影响分析以及建议处理方式（KEEP、MODIFY、RESTORE、DELETE）。
3.《》，这是需要补充的内容清单，请结合冻结设计、历史审计结论、开发方案和当前补充项进行分析，重点检查是否仍存在架构漂移风险、实现歧义、责任边界不清、验收缺口、测试盲区或未来可能再次偏离设计的隐患；不要新增与冻结架构无关的功能，不要改变既定设计目标，只补充实现约束、数据契约、接口契约、决策链路约束、验收标准和回归要求；对于每个补充项，明确其必要性、影响范围以及与冻结设计的关系，区分 KEEP、MODIFY、RESTORE、DELETE；最终生成独立 md 文档正文，确保开发完成后既能验证功能正确，也能验证冻结架构仍然真正参与最终决策，而不是仅存在于代码中。
4.《》，这是补充的信息，核心逻辑、决策链路、接口契约、数据契约和架构约束不允许凭空创造、推测、简化、替换或重新解释，必须严格以已冻结设计、审计结论和开发方案为唯一依据进行开发；开发前必须先核对当前代码是否存在历史遗留逻辑、兼容逻辑、旁路逻辑、失效逻辑、重复实现或架构漂移，不得因为代码已经存在而默认其正确；凡涉及核心决策链路的修改，必须验证相关功能不仅存在于代码中，而且真实参与最终决策，不允许出现“功能存在但不生效”“功能生效但被后续覆盖”“功能实现但未进入主链路”等情况；禁止新增未在设计中明确说明的规则、权重、门控、兜底、特判、兼容层或隐式行为；禁止保留与冻结架构冲突但未被本轮开发显式使用的历史逻辑；如发现当前代码与冻结设计不一致，必须在开发报告中明确标注 Expected、Actual、Impact 和处理方式（KEEP、MODIFY、RESTORE、DELETE），不得自行决定保留；开发时必须遵循 D:\Programs\github\lingua_1\docs\CODING\vibe coding代码规范，并进行 Frozen Architecture Verification，确认最终运行链路与冻结设计一致，而不仅仅是功能测试通过。

5. 请清理进程以后启动节点端，使用 D:\Programs\github\lingua_1\test wav\dialog_200 的音频文件对本轮开发进行测试（最多15分钟，能测几条是几条），出具开发报告和测试报告，需要包括确认核心逻辑是否符合设计，前后调整内容，识别质量和性能的数据，以及测试结果抽样；除功能测试外，必须进行 Frozen Architecture Verification，核对本轮开发涉及的冻结架构、接口契约、数据契约和决策链路是否仍然成立，确认新增逻辑是否真正参与最终决策而非仅存在于代码中，确认是否存在功能失效、决策被覆盖、门控失效、死代码、重复实现、隐藏降级、旁路执行或架构漂移现象；对于每个核心功能必须同时验证“功能存在”和“功能生效”，说明其输入、输出、决策位置、最终影响路径以及是否被后续模块覆盖；如果本轮功能声称参与核心决策，必须提供反事实验证：禁用、绕过或移除该功能后，相关语义样例应出现可解释退化，否则不能证明该功能真实生效。如发现与冻结设计不一致的情况，必须列出 Expected、Actual、Impact、Severity 以及 KEEP、MODIFY、RESTORE、DELETE 建议，并在结论部分给出 Architecture Compliance 评估，明确本轮开发是否真正符合冻结架构，而不仅仅是通过功能测试。
6. 这是本轮最终验收通过的开发报告、测试报告、反事实验证报告和相关文档，请结合实际代码进行 Frozen Architecture Update 和 Documentation Alignment Audit，确认本轮开发已经真实参与最终决策且不存在架构漂移、责任边界漂移、决策链路漂移、功能失效、功能被覆盖、隐藏降级、旁路执行、死代码或重复实现；核对冻结架构、接口契约、数据契约、Ownership Matrix、Diagnostics、Trace、Regression Gate、Semantic Acceptance、Counterfactual Test 与实际运行链路是否一致；如发现设计、代码、测试和文档之间存在差异，必须明确指出并给出 KEEP、MODIFY、RESTORE、DELETE 建议；对于本轮确认有效的设计、决策链路、接口、数据结构、Diagnostics、Trace、测试基线和回归样例，必须同步更新相关架构文档、Contract 文档、Diagnostics 文档、Regression 文档和 Freeze 文档，清理或归档已废弃的设计、字段、流程、门控、阈值和历史文档，确保项目始终只有一个有效的 SSOT（Single Source of Truth）；最终出具冻结更新报告、文档更新报告和文档清理报告，并明确本轮开发后哪些内容进入冻结架构、哪些内容属于历史遗留、哪些内容已经废弃，以及未来修改这些内容时必须重新执行冻结流程。

# 2. 调度服务器（在新窗口）
cd d:\Programs\github\lingua_1\electron_node\scripts
.\kill_residual_processes.ps1
cd D:\Programs\github\lingua_1\central_server\scheduler
cargo clean
powershell -ExecutionPolicy Bypass -File .\scripts\redis_clear_lingua.ps1
cd D:\Programs\github\lingua_1
.\scripts\start_scheduler.ps1

# 3. 模型管理
cd D:\Programs\github\lingua_1
.\scripts\start_model_hub.ps1

# redis 集群
cd D:\Programs\github\lingua_1\central_server\scheduler
.\scripts\phase2_cluster_acceptance.ps1

# 在仓库根目录执行
cd D:\Programs\github\lingua_1
.\scripts\start_all_services_with_terminals.ps1


访问仪表盘：
http://localhost:5010/dashboard
查看统计数据 API：
http://localhost:5010/api/v1/stats


# 4. Web 客户端（在新窗口）
cd D:\Programs\github\lingua_1\webapp\web-client
.\clean_cache.ps1
npm install
npm run build
cd D:\Programs\github\lingua_1
.\scripts\start_webapp.ps1

#导出日志：http://localhost:9001/export-logs-simple.html



cd D:\Programs\github\lingua_1\electron_node\electron-node 
Get-Location   
npm run build:main  

0.
1.
2.embedding实装，服务模式+连续播放
3.yourtts实装
1.model hub改造成多实例
2.日志服务器，统计节点性能，错误日志
3.公司指定服务器
4.会议室实装，连续播放
4.第三方API，SDK
4.收费模式
5.模型训练

请将D:\Programs\github\lingua_1\central_server\docs和D:\Programs\github\lingua_1\central_server\scheduler\docs的docs文档进行合并整理，按每个模块的docs只放在自己模块里，根据实际代码补充新增功能，移除过期（与当前代码不符）和废弃的内容，并能合并的就合并，文档大小最好不要超过500行。测试报告之类的文件全都移除

现在请对D:\Programs\github\lingua_1\electron_node\services\faster_whisper_vad的代码文件和测试文件进行整理，拆分超过500行的大文件，合并重复的逻辑，只对代码进行迁移，不要改变任何接口，参数和返回结果，也不要改动任何代码逻辑。异步方法之间如果有必要的话采用状态机进行管理，然后把Import都移动到文件头部,然后进行单元测试，修复单元测试时，不要考虑兼容，不要打补丁或新增代码逻辑来添加保险或者兜底机制，并避免错误调用旧路径旧方法


请实现这个功能，添加流程日志，进行单元测试，并更新文档。

不要考虑兼容，这个项目没有上线，没有用户，只需要保持代码简洁，做好单元测试，然后更新文档

请将现在的utterance聚合流程，从ASR返回结果到发送给语义修复服务，具体到每一个调用的方法，整理出来，看看有没有重复的调用或者错误的调用会导致不必要的开销，然后给我一个文档交给决策部门审议

这个项目的开发力量不足，无法对每个模块每个流程都做到滚瓜烂熟。我希望代码逻辑尽可能简单易懂，方便找到问题，而不是添加一层又一层的保险措施来掩盖问题，或者在应该略有调整的地方直接新增一条业务路径（严格禁止），如果不是必须的逻辑，就不要用打补丁的方式来解决，最好能用架构设计解决，也就是允许存在复杂的“业务现象”，但不允许存在复杂的“控制流”，如果遇到无法避免的问题请先告诉我

请在本次改造中根据实际代码进行谨慎调整，不要新增不必要的流程路径，产生新的重复逻辑或者冗余的处理过程。我希望代码逻辑尽可能简单易懂，方便找到问题，而不是添加一层又一层的保险和兜底措施来掩盖问题，也不要考虑兼容，该移除的代码逻辑直接移除，这个项目没有上线，没有用户，只需要保持代码简洁，如果不是必须的逻辑，就不要用打补丁的方式来解决，最好能用架构设计解决，如果遇到无法避免的问题请先告诉我

请把web端对音频的接收和发送、播放按钮的前后流程，具体到每个调用方法，整理成文档，让我交给决策部门

这是本次改造之后的的调度服务器节点注册，节点管理和任务管理流程，请看一下有没有重复或者矛盾的逻辑，有没有遗漏或者可以优化的地方

节点端和调度服务器最近进行了重构，请参考D:\Programs\github\lingua_1\expired\lingua_1-main的备份代码进行接口或者数据格式的调整，备份代码是可以通过集成测试的。我希望代码逻辑尽可能简单易懂，方便找到问题，而不是添加一层又一层的保险措施来掩盖问题，或者在应该略有调整的地方直接新增一条业务路径（严格禁止），如果不是必须的逻辑，就不要用打补丁的方式来解决，最好能用架构设计解决。



现在我们开始进行一次语音识别稳定性测试。
我会先读一两句比较短的话，用来确认系统不会在句子之间随意地把语音切断，或者在没有必要的时候提前结束本次识别。

接下来这一句我会尽量连续地说得长一些，中间只保留自然的呼吸节奏，不做刻意的停顿，看看在超过十秒钟之后，系统会不会因为超时或者静音判定而强行把这句话截断，从而导致前半句和后半句在节点端被拆成两个不同的 job，甚至出现语义上不完整、读起来前后不连贯的情况。

如果这次的长句能够被完整地识别出来，而且不会出现半句话被提前发送或者直接丢失的现象，那就说明我们当前的切分策略和超时规则是基本可用的。
否则，我们还需要继续分析日志，找出到底是在哪一个环节把我的语音吃掉了。



我想对超长语音做出更好的处理，以提升用户体验：假设场景是35秒的长语音，被调度服务器拆成4个job，进入节点端后被AudioAggregator切分成多个片段：job0_1:3秒，job0_2:3秒，job0_3:4秒，job1_1:3秒，job1_2:3秒，job1_3:4秒，job2_1:3秒，job2_2:3秒，job2_3:3秒，job2_4:1秒，job3_1:5秒。这时虽然AudioAggregator会一直等到job3带着手动截断或者pause截断的标识来了才算完成合并，但在这之前，这些片段已经被分别合并送入ASR了，其中job0_1+job0_2=6秒，job0_3+job1_1=7秒，job1_2+job1_3=7秒，job2_1+job2_2=6秒，job2_3+job2_4+job3_1=9秒还是会出现5个语音识别文本的切片，如果utterance拼接需要等到这个5个切片都识别完成再发给语义修复服务，也会有一个很长的延迟，再加上语义修复和NMT和TTS处理的时间，用户可能需要等30秒以上才有结果，这就违背了流式处理的初衷。所以我希望在utterance中，将语音识别文本与job从头部对齐，也就是job0_1+job0_2=6秒作为job0的结果，job0_3+job1_1=7秒和job1_2+job1_3=7秒合并作为job1的结果，job2_1+job2_2=6秒和job2_3+job2_4+job3_1=9秒作为job2的结果进行下一步处理，发送给语义修复/NMT/TTS，返回web端，如果没有语音识别文本是以job3开头的，就把job3视为合并入job2了。也就是audioAggregator会将Job按能量切分，但utterance会将语音识别文本合并，只要第一个文本片段属于哪个job，就将这个文本片段作为该job的结果返回，哪怕这个文本片段是由多个超过5秒语音片段组成的。这样做的目的是确保切片数量不会超过job容器数量，不会产生文本丢失的情况



我完成了本次集成测试，阅读的文本是:"现在我们开始进行一次语音识别稳定性测试。
我会先读一两句比较短的话，用来确认系统不会在句子之间随意地把语音切断，或者在没有必要的时候提前结束本次识别。

接下来这一句我会尽量连续地说得长一些，中间只保留自然的呼吸节奏，不做刻意的停顿，看看在超过十秒钟之后，系统会不会因为超时或者静音判定而强行把这句话截断，从而导致前半句和后半句在节点端被拆成两个不同的 job，甚至出现语义上不完整、读起来前后不连贯的情况。

如果这次的长句能够被完整地识别出来，而且不会出现半句话被提前发送或者直接丢失的现象，那就说明我们当前的切分策略和超时规则是基本可用的。
否则，我们还需要继续分析日志，找出到底是在哪一个环节把我的语音吃掉了。"，返回结果是："",请检查节点端日志D:\Programs\github\lingua_1\electron_node\electron-node\logs\electron-main.log，看看每个job在各服务里的处理过程，输入是什么输出是什么




备份代码：
#终端1: 调度服务器
cd D:\Programs\github\lingua_1\expired\lingua_1-main\central_server\scheduler
cargo run --release

#终端2: 节点端
cd D:\Programs\github\lingua_1\expired\lingua_1-main\electron_node\electron-node
npm install
npm run build:main
npm start

#终端3: Web端
cd D:\Programs\github\lingua_1\expired\lingua_1-main\webapp\web-client
npm install
npm run dev



# 运行 Job 清理测试
cargo test job_cleanup_test --no-fail-fast

# 运行所有测试
cargo test --lib

# 编译检查
cargo check --lib

# Lexicon Schema V2 Only 冻结路径（勿用 lexicon:build — 已 fail-fast）
cd D:\Programs\github\lingua_1\electron_node\electron-node
npm run lexicon:validate -- --input data/lexicon/zh_asr_confusions_seed_high_quality.jsonl
npm run lexicon:build:v2-shadow
npm run lexicon:prepare:v3-runtime -- --force
npm run lexicon:gate:v3-runtime
npm run lexicon:rebuild-sqlite
# 验收：dialog_200 / 12句专项 / test:lexicon-patch-e2e（需节点 :5020）
npm run test:schema-v2-seed-acceptance-12
npm run test:fw-detector

