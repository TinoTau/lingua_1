# 备份代码语义修复服务说明

**日期**: 2026-01-21 03:00  
**问题**: 备份代码节点端找不到语义修复服务  
**原因**: ✅ **服务是共享的，两套代码共用同一个services目录**

---

## 🔍 发现的情况

### 1. 服务目录结构

**备份代码的services**:
```
D:\Programs\github\lingua_1\expired\lingua_1-main\electron_node\services\
  - faster_whisper_vad\
  - nmt_m2m100\
  - piper_tts\
  - semantic_repair_en_zh\  ✅ 有完整代码
  - semantic_repair_zh\     ✅ 有完整代码
  - speaker_embedding\
  - your_tts\
```

**当前代码的services**:
```
D:\Programs\github\lingua_1\electron_node\services\
  - faster_whisper_vad\
  - nmt_m2m100\
  - piper_tts\
  - semantic_repair_en_zh\  ✅ 有完整代码
  - semantic_repair_zh\     ✅ 有完整代码
  - speaker_embedding\
  - your_tts\
```

---

## 💡 关键发现

### installed.json指向主项目

**备份代码的`installed.json`**:
```json
{
  "semantic-repair-zh": {
    "install_path": "D:/Programs/github/lingua_1/electron_node/services/semantic_repair_zh"
  },
  "semantic-repair-en-zh": {
    "install_path": "D:/Programs/github/lingua_1/electron_node/services/semantic_repair_en_zh"
  }
}
```

**注意**: 路径指向**主项目**，不是备份代码路径！

---

## 📊 实际情况

### 服务是共享的

**设计**:
- 语义修复服务的模型文件很大（2GB+）
- 不会为每套代码单独复制一份
- **两套代码共享同一个services目录**

**位置**:
```
D:\Programs\github\lingua_1\electron_node\services\
  └─ semantic_repair_en_zh\  ← 两套代码都使用这个
  └─ semantic_repair_zh\     ← 两套代码都使用这个
```

---

## ✅ 验证

### 两套代码都有完整的语义修复服务

**备份代码**:
```
D:\Programs\github\lingua_1\expired\lingua_1-main\electron_node\services\semantic_repair_en_zh\
  ✅ service.py (主服务)
  ✅ engines\ (引擎实现)
  ✅ processors\ (处理器)
  ✅ 完整的代码结构
```

**当前代码**:
```
D:\Programs\github\lingua_1\electron_node\services\semantic_repair_en_zh\
  ✅ service.py (主服务)
  ✅ engines\ (引擎实现)
  ✅ processors\ (处理器)
  ✅ 完整的代码结构
```

**结论**: ✅ **两套代码都有完整的语义修复服务**

---

## 🎯 为什么找不到？

### 可能的原因

1. **服务未启动**
   - 语义修复服务是可选服务
   - 需要手动启动或在配置中启用

2. **节点端未加载服务**
   - 检查节点端启动日志
   - 看是否有"Service loaded"信息

3. **配置文件路径问题**
   - `installed.json`路径指向主项目
   - 备份代码的节点端可能无法正确加载

---

## 🔧 解决方案

### 方案1: 使用主项目的services目录（推荐）

**原理**: 备份代码已经配置为使用主项目的services

**操作**: 不需要任何修改，直接启动即可

**验证**:
```powershell
# 启动备份代码节点端
cd D:\Programs\github\lingua_1\expired\lingua_1-main\electron_node\electron-node
npm start

# 查看日志，应该看到服务加载信息
```

---

### 方案2: 修改installed.json指向备份代码（不推荐）

**修改**: `D:\Programs\github\lingua_1\expired\lingua_1-main\electron_node\services\installed.json`

**将所有路径从**:
```
D:/Programs/github/lingua_1/electron_node/services/...
```

**改为**:
```
D:/Programs/github/lingua_1/expired/lingua_1-main/electron_node/services/...
```

**缺点**:
- ⚠️ 需要修改多个路径
- ⚠️ 会导致模型重复（浪费磁盘空间）
- ⚠️ 不是设计的意图

---

### 方案3: 复制主项目的语义修复服务（不推荐）

**如果真的需要独立的services**:

```powershell
# 复制语义修复服务
Copy-Item "D:\Programs\github\lingua_1\electron_node\services\semantic_repair_en_zh" `
          "D:\Programs\github\lingua_1\expired\lingua_1-main\electron_node\services\" `
          -Recurse -Force

# 修改installed.json路径
```

**缺点**: 占用大量磁盘空间（4GB+）

---

## 🧪 测试方法

### 检查服务是否正常加载

**启动节点端后，查看日志**:

```powershell
# 备份代码节点端日志（如果有）
Get-ChildItem "D:\Programs\github\lingua_1\expired\lingua_1-main\electron_node\electron-node\logs" -Filter "*.log" | Select-Object -First 1 | Get-Content -Tail 50
```

**查找关键信息**:
- "Service loaded: semantic-repair-en-zh"
- "Service started: semantic-repair-zh"

---

## 📋 正确的启动顺序

### 首次启动备份代码

由于服务是共享的，应该：

1. **确保主项目的services目录完整**
   ```powershell
   ls D:\Programs\github\lingua_1\electron_node\services\semantic_repair_en_zh
   # 应该看到service.py等文件
   ```

2. **启动备份代码节点端**
   ```powershell
   cd D:\Programs\github\lingua_1\expired\lingua_1-main\electron_node\electron-node
   npm install
   npm run build:main
   npm start
   ```

3. **节点端会自动加载主项目services目录的服务**

---

## ⚠️ 关键理解

### Services是共享资源

```
主项目代码:
  electron_node/services/ ← 实际的服务代码和模型
       ↑
       | (installed.json指向这里)
       |
备份代码:
  expired/lingua_1-main/
    electron_node/services/
      installed.json → 指向主项目services
```

**这是正常的设计**:
- ✅ 避免重复存储大型模型（节省磁盘）
- ✅ 两套代码可以共享服务
- ✅ 更新服务时只需更新一处

---

## 🎯 如果真的找不到服务

### 检查清单

1. **主项目services目录是否完整**:
   ```powershell
   Test-Path "D:\Programs\github\lingua_1\electron_node\services\semantic_repair_en_zh\service.py"
   # 应返回: True
   ```

2. **installed.json是否存在**:
   ```powershell
   Test-Path "D:\Programs\github\lingua_1\expired\lingua_1-main\electron_node\services\installed.json"
   # 应返回: True
   ```

3. **节点端是否正确读取配置**:
   - 查看启动日志
   - 看是否有"Failed to load service"错误

---

## 💡 总结

### 关键点

1. **语义修复服务存在** ✅
   - 在主项目的services目录中
   - 有完整的代码和模型

2. **备份代码会使用主项目的服务** ✅
   - 通过installed.json配置
   - 这是正常的设计

3. **不需要特殊处理** ✅
   - 直接启动备份代码节点端即可
   - 服务会自动加载

### 如果还是找不到

请提供：
1. 备份代码节点端的启动日志
2. 具体的错误信息
3. 在哪里看到"找不到"的提示

---

**建议**: 先正常启动备份代码，查看节点端日志，确认服务加载情况。
