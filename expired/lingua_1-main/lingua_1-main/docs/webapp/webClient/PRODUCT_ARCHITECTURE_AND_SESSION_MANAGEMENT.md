# Web 端产品架构与 Session 管理

**日期**: 2025-01-XX  
**目的**: 说明当前 Web 端的产品形态、多用户服务机制和 Session 分配策略

---

## 📋 当前产品形态

### 1. 架构概览

**当前实现**: 直接连接模式

```
┌──────────────┐
│  Web Client  │ (用户 A)
│              │
│  WebSocket ──┼──┐
└──────────────┘  │
                  │
┌──────────────┐  │
│  Web Client  │  │  (用户 B)
│              │  │
│  WebSocket ──┼──┼──┐
└──────────────┘  │  │
                  │  │
┌──────────────┐  │  │
│  Web Client  │  │  │  (用户 C)
│              │  │  │
│  WebSocket ──┼──┼──┼──┐
└──────────────┘  │  │  │
                  ▼  ▼  ▼
        ┌─────────────────────┐
        │     Scheduler       │
        │                     │
        │  - SessionManager   │ (管理所有 Session)
        │  - ConnectionMgr    │ (管理所有 WebSocket 连接)
        │  - RoomManager      │ (会议室模式)
        │  - Dispatcher       │ (任务分发)
        └─────────────────────┘
```

### 2. 产品特点

#### ✅ 已实现的特点

- **直接连接**: Web 客户端直接通过 WebSocket 连接到 Scheduler
- **无状态连接**: 每个连接都是独立的，不需要登录/认证
- **匿名 Session**: Session ID 由服务器生成，不与用户身份绑定
- **多用户支持**: 支持任意数量的并发用户连接
- **Session 隔离**: 每个用户的 Session 完全独立，互不干扰
- **会议室模式**: 支持多个用户在同一房间内协作

#### ⚠️ 当前限制

- **无用户认证**: 目前没有登录/注册机制
- **无租户隔离**: 虽然支持 `tenant_id`，但 Web 客户端目前不使用
- **无持久化**: Session 仅存储在内存中，服务重启后丢失
- **无会话恢复**: 连接断开后，Session 会被清理，无法恢复

---

## 🔧 Session 分配机制

### 1. Session 创建流程

**触发时机**: Web 客户端发送 `session_init` 消息时

```typescript
// web-client/src/websocket_client.ts
async connectOneWay(srcLang: string, tgtLang: string): Promise<void> {
  this.ws = new WebSocket(this.url);
  
  this.ws.onopen = () => {
    // 发送会话初始化消息
    const initMessage = {
      type: 'session_init',
      client_version: 'web-client-v1.0',
      platform: 'web',
      src_lang: srcLang,
      tgt_lang: tgtLang,
      // tenant_id: undefined, // Web 客户端目前不提供 tenant_id
    };
    this.ws!.send(JSON.stringify(initMessage));
  };
}
```

**服务器处理**:

```rust
// scheduler/src/websocket/session_message_handler.rs
let session = state.session_manager.create_session(
    client_version,
    platform,  // "web"
    src_lang,
    tgt_lang,
    dialect,
    features,
    tenant_id,  // None for web clients
    mode,
    lang_a,
    lang_b,
    auto_langs,
    trace_id,
).await;
```

### 2. Session ID 生成规则

**格式**: `s-{UUID前8位大写}`

**代码**:

```rust
// scheduler/src/session.rs
pub async fn create_session(...) -> Session {
    // 生成 Session ID: s-{UUID前8位}
    let session_id = format!("s-{}", Uuid::new_v4().to_string()[..8].to_uppercase());
    
    let session = Session {
        session_id: session_id.clone(),
        // ... 其他字段
        tenant_id: None,  // Web 客户端目前不使用 tenant_id
        // ...
    };
    
    // 存储到内存中
    let mut sessions = self.sessions.write().await;
    sessions.insert(session_id, session.clone());
    session
}
```

**示例**:
- `s-A1B2C3D4`
- `s-E5F6G7H8`
- `s-9I0J1K2L`

### 3. Session 生命周期

```
1. 客户端建立 WebSocket 连接
   ↓
2. 客户端发送 session_init 消息
   ↓
3. 服务器创建 Session，生成 session_id
   ↓
4. 服务器注册 WebSocket 连接到 SessionConnectionManager
   ↓
5. 服务器发送 session_init_ack（包含 session_id）
   ↓
6. 客户端收到 session_id，开始使用
   ↓
7. ... 正常使用（发送音频、接收翻译结果） ...
   ↓
8. 客户端断开连接 / 发送 session_close
   ↓
9. 服务器清理：
   - 从 SessionConnectionManager 注销连接
   - 从 SessionManager 移除 Session
   - 清理 ResultQueue
   - 清理 Room（如果适用）
```

---

## 🌐 多用户服务机制

### 1. SessionManager（会话管理）

**职责**: 管理所有活跃的 Session

**存储**: 内存 HashMap

```rust
// scheduler/src/session.rs
pub struct SessionManager {
    sessions: Arc<RwLock<HashMap<String, Session>>>,  // key: session_id
}
```

**容量**: 
- 理论上限: 取决于服务器内存（每个 Session ~1-2 KB）
- 实际限制: WebSocket 连接数限制（OS 级别，通常几万到几十万）

### 2. SessionConnectionManager（连接管理）

**职责**: 管理所有客户端的 WebSocket 连接

**存储**: 内存 HashMap

```rust
// scheduler/src/connection_manager.rs
pub struct SessionConnectionManager {
    connections: Arc<RwLock<HashMap<String, mpsc::UnboundedSender<Message>>>>,
}
```

**功能**:
- `register(session_id, sender)`: 注册连接
- `unregister(session_id)`: 注销连接
- `send(session_id, message)`: 向指定 Session 发送消息
- `get(session_id)`: 获取连接的发送器

### 3. 并发处理能力

**当前架构**:
- ✅ **异步非阻塞**: 使用 Tokio 异步运行时
- ✅ **多线程**: Tokio 运行时可以在多个线程上调度任务
- ✅ **内存隔离**: 每个 Session 的数据完全独立

**性能估算**:
- 单个 Session 内存: ~10-50 KB（包括连接状态、缓冲区）
- 10,000 并发 Session: ~100-500 MB
- 100,000 并发 Session: ~1-5 GB

**瓶颈**:
- ⚠️ **WebSocket 连接数**: 操作系统文件描述符限制
- ⚠️ **内存**: 大量并发连接的内存占用
- ⚠️ **CPU**: 消息序列化/反序列化（相对较小）

---

## 📊 用户隔离机制

### 1. Session 级别隔离

**特点**:
- ✅ 每个用户有独立的 `session_id`
- ✅ 每个用户有独立的 WebSocket 连接
- ✅ 翻译结果只发送给对应的 Session
- ✅ 音频数据不会混淆

**代码示例**:

```rust
// 发送翻译结果
state.session_connections.send(
    &session_id,  // 只发送给指定的 Session
    Message::Text(result_json)
).await;
```

### 2. 会议室模式隔离

**特点**:
- ✅ 房间码隔离（6位数字，如 `483920`）
- ✅ 只有知道房间码的用户才能加入
- ✅ 房间内成员可以相互通信
- ✅ 房间之间完全隔离

**代码示例**:

```rust
// 加入房间时需要提供房间码
match state.room_manager.join_room(&room_code, session_id, ...).await {
    Ok(()) => { /* 成功加入 */ }
    Err(RoomError::RoomNotFound) => { /* 房间不存在 */ }
    Err(RoomError::InvalidRoomCode) => { /* 房间码无效 */ }
}
```

### 3. 租户隔离（预留功能）

**当前状态**: 
- ✅ 代码层面支持 `tenant_id`
- ❌ Web 客户端目前不提供 `tenant_id`
- ✅ API Gateway 支持租户隔离

**使用场景**:
- 第三方应用通过 API Gateway 接入
- 企业用户需要数据隔离
- 多租户 SaaS 服务

**代码示例**:

```rust
// Session 结构支持 tenant_id
pub struct Session {
    pub session_id: String,
    pub tenant_id: Option<String>,  // 租户 ID（可选）
    // ...
}
```

---

## 🔄 实际使用场景

### 场景 1: 单用户使用

```
用户 A 打开 Web 客户端
  ↓
建立 WebSocket 连接
  ↓
发送 session_init (src_lang="zh", tgt_lang="en")
  ↓
服务器创建 Session (session_id="s-A1B2C3D4")
  ↓
用户 A 开始使用翻译功能
  ↓
所有翻译结果都发送给 session_id="s-A1B2C3D4"
```

### 场景 2: 多用户并发使用

```
用户 A: WebSocket 连接 → Session "s-A1B2C3D4"
用户 B: WebSocket 连接 → Session "s-E5F6G7H8"
用户 C: WebSocket 连接 → Session "s-9I0J1K2L"
...
（所有用户独立运行，互不干扰）
```

### 场景 3: 会议室模式

```
用户 A: 创建房间 (room_code="483920", preferred_lang="en")
  ↓
服务器创建房间，用户 A 加入
  ↓
用户 B: 加入房间 (room_code="483920", preferred_lang="zh")
  ↓
服务器将用户 B 添加到房间
  ↓
用户 A 说话（中文）→ 翻译结果路由：
  - 英文翻译 → 发送给用户 A (preferred_lang="en")
  - 中文翻译 → 发送给用户 B (preferred_lang="zh")
```

### 场景 4: 通过 API Gateway（第三方应用）

```
第三方应用 (tenant_id="company-abc")
  ↓
通过 API Gateway 接入
  ↓
API Gateway 验证 API Key → 获取 tenant_id
  ↓
转发请求到 Scheduler（附带 tenant_id）
  ↓
Scheduler 创建 Session (tenant_id="company-abc")
  ↓
所有该租户的 Session 可以统一管理
```

---

## 🎯 未来扩展方向

### 1. 用户认证系统

**目标**: 
- 支持用户登录/注册
- Session 与用户身份绑定
- 支持会话历史记录

**实现**:
```rust
pub struct Session {
    pub session_id: String,
    pub user_id: Option<String>,  // 新增：用户 ID
    pub tenant_id: Option<String>,
    // ...
}
```

### 2. Session 持久化

**目标**:
- 将 Session 存储到数据库（Redis/PostgreSQL）
- 支持会话恢复（重连后恢复状态）
- 支持会话历史记录

**实现**:
- 使用 Redis 存储 Session 状态
- 支持 Session 过期（TTL）
- 支持跨服务器共享（分布式部署）

### 3. 租户隔离增强

**目标**:
- Web 客户端支持租户登录
- 按租户限制资源使用
- 按租户统计和计费

**实现**:
```typescript
// web-client 支持租户
const initMessage = {
  type: 'session_init',
  tenant_id: user.tenantId,  // 新增
  // ...
};
```

### 4. 负载均衡和水平扩展

**目标**:
- 支持多个 Scheduler 实例
- 使用负载均衡器分发连接
- 支持 Session 迁移

**架构**:
```
            ┌─────────────┐
            │ Load Balancer│
            └──────┬───────┘
                   │
        ┌──────────┼──────────┐
        ▼          ▼          ▼
    Scheduler  Scheduler  Scheduler
      (实例1)    (实例2)    (实例3)
```

---

## ✅ 总结

### 当前产品形态

| 特性 | 说明 |
|------|------|
| **连接方式** | ✅ 直接 WebSocket 连接到 Scheduler |
| **用户认证** | ❌ 无（匿名 Session） |
| **Session 生成** | ✅ 服务器自动生成（UUID） |
| **多用户支持** | ✅ 支持（内存 HashMap） |
| **用户隔离** | ✅ Session 级别隔离 |
| **会议室模式** | ✅ 房间码隔离 |
| **租户支持** | ⚠️ 代码支持，Web 客户端不使用 |
| **持久化** | ❌ 无（仅内存） |

### Session 分配规则

1. **生成时机**: 客户端发送 `session_init` 消息时
2. **生成规则**: `s-{UUID前8位大写}`
3. **存储位置**: Scheduler 内存（`SessionManager`）
4. **生命周期**: 连接建立 → Session 创建 → 连接断开 → Session 清理
5. **隔离机制**: Session ID 唯一标识，完全隔离

### 服务能力

- ✅ **并发支持**: 理论无上限（受内存和 OS 限制）
- ✅ **性能**: 异步非阻塞，高性能
- ✅ **隔离**: Session 级别完全隔离
- ⚠️ **持久化**: 无（服务重启后丢失）
- ⚠️ **恢复**: 无（连接断开后无法恢复）

---

## 🔗 相关文档

- [系统架构文档](../ARCHITECTURE.md)
- [面对面模式连接机制](./FACE_TO_FACE_MODE_CONNECTION.md)
- [会议室模式连接与路由](../webRTC/ROOM_MODE_CONNECTION_AND_ROUTING.md)
- [API Gateway 设计](../api_gateway/PUBLIC_API_DESIGN.md)

---

**最后更新**: 2025-01-XX

