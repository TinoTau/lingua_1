# LEGACY_RECOVER_ONLY

此目录为 **Legacy ASR Recover** 词库 bundle，**不是** FW Repair V4 主链 runtime。

| 项 | 值 |
|----|-----|
| 用途 | Legacy Recover / `LexiconRuntime` V1 |
| schema | `final-v1` (~2k 词) |
| FW 主链 | 使用 `../v3/`（`LexiconRuntimeV2`） |

**禁止** 将此目录当作 FW 词库编辑或部署目标。
